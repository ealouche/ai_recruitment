# Configuration Azure Blob Storage
# Remplacez par votre chaîne de connexion Azure Storage
AZURE_STORAGE_CONNECTION_STRING="DefaultEndpointsProtocol=https;AccountName=VOTRE_COMPTE;AccountKey=VOTRE_CLE;EndpointSuffix=core.windows.net"

# Nom du container Azure Blob (sera créé automatiquement s'il n'existe pas)
AZURE_CONTAINER_NAME="cv-uploads"

# Backend de stockage : "local" ou "azure"
STORAGE_BACKEND="azure"

# Configuration CORS (optionnel - déjà défini dans config.py)
# ALLOWED_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173"]

# Configuration upload (optionnel - déjà défini dans config.py)
# MAX_FILE_SIZE=5242880  # 5MB
# ALLOWED_FILE_TYPES=["application/pdf"] 
